# smart_meals
An app that generates recipes from a user specifying ingredients that are on hand. It is being built using Node.js and the @xenova/transformers library to perform local semantic text embedding, demonstrating an efficient, server-side-only AI pipeline without needing external paid embedding APIs.
